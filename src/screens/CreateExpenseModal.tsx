import {Text, View} from 'react-native';
import React, {Component} from 'react';

export class CreateExpenseModal extends Component {
  render() {
    return (
      <View>
        <Text>CreateExpenseModal</Text>
      </View>
    );
  }
}

export default CreateExpenseModal;
